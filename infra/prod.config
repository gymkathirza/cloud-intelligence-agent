bucket         = "github-cia-unique-tf-state-bucket"
key            = "prod/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
use_lockfile = true
dynamodb_table = "github-cia-unique-terraform-lock" # Prevents two people from running it at once
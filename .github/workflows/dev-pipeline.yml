name: "Dev Deployment Cloud Agent CI/CD"

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual run'
        required: false
        default: 'Manual Dev trigger'

permissions:
  id-token: write # Required for OIDC
  contents: read  # Required for Checkout

jobs:
  terraform-and-test:
    runs-on: ubuntu-latest
    # Set the default directory for ALL steps in this job
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        working-directory: ./infra
        # Force a change to local backend for dev work
        run: terraform init -backend-config=dev.config -migrate-state

      # Dev Environment to plan only, no apply
      - name: Terraform Plan
        working-directory: ./infra
        run: terraform plan -var-file="envs/dev.tfvars"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        working-directory: ./
        run: |
          pip install -r requirements.txt

      - name: Run Connection Test
        working-directory: ./
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: python src/verify_connection.py